/* ==========================================================================
   the-work — this page's own motion.

   The shared controller in scripts/motion.js runs first and owns the page
   chrome, the deal-in and the scroll bus. This module adds only what is
   specific to this page, and tears down everything it registers.
   ========================================================================== */

export {};
