# Handoff

- **Baton:** Bee, 2026-08-25; paused before any further ISO build
- **Where it stands:** Fedora 44 Anaconda generic-ISO installer now reaches real `bootc install to-filesystem` deployment. Payload import succeeds after the Anaconda service sets `TMPDIR=/mnt/sysimage`; the remaining failure is bootc's `Verifying empty rootfs: ... Found empty directory: <UUID>`. The diagnostic wrapper proved `/mnt/sysimage/boot` is the only visible root child before bootc. The UUID is bootc's own temporary directory, created because `TMPDIR=/mnt/sysimage` points directly at the target root. The latest unbuilt change sets `TMPDIR=/mnt/sysimage/var`; it has not been validated.
- **Latest test evidence:** `projects/advisor-os/artifacts/fixed-auto-diagnostic3.log` is ignored local evidence. It shows `/mnt/sysimage/boot/efi` and `/mnt/sysimage/boot/lost+found` before the error, then the UUID empty-directory error. Source review of current bootc confirms its temporary directory is allocated before `require_empty_rootdir()` and that empty directories are rejected.
- **Next move:** Do not rebuild yet. Obtain and review the external research report at `/home/chris/work/secureprospective-advisor-os/projects/advisor-os/artifacts/bootc-installer-plan.md`. Decide whether the supported fix is a separate `/var` mount, a different temporary-directory mount, or abandoning this Anaconda-to-filesystem path for a supported bootc installer path. Then make one change and run one controlled install test.
- **Blocked on:** Choosing a supported, repeatable bootc/Anaconda installation design. The current `TMPDIR=/mnt/sysimage/var` hypothesis is plausible but unverified. The temporary diagnostic wrapper in `projects/advisor-os/installer/bootc-wrapper.sh` must be removed before any release image.
- **Preservation rules:** Do not stage, overwrite, stash, or delete unrecognized files. In particular preserve `docs/ADVISOR_OS_LANDING_CONTENT.md`, all untracked knowledge files, the live-test log, and ignored diagnostic artifacts. Do not claim encrypted-install acceptance until installation, reboot, unlock, KDE login, Brave/PWA, and printer workflow pass.
- **Build/test state:** No build process is running. Latest pushed project commit is `96a9077`; current worktree contains intentional installer/live-test edits plus untracked content. QEMU and image-builder are available.
