# Handoff

- **Baton:** Bee, 2026-08-25; working POC ISO now exists and has passed automated clean install plus installed-disk boot/login in QEMU
- **Where it stands:** `projects/advisor-os/artifacts/iso/advisor-os-installer-latest.iso` was built successfully. Working test is `qemu-boot-skip-07`: UEFI, virtual TPM, fresh 80 GiB disk, 8 GiB RAM. Anaconda created LUKS2 root and ext4 `/boot`; bootc imported the 6.8 GiB payload, completed deployment, and Anaconda reported `Installation complete!`. The installed disk rebooted, accepted LUKS passphrase `advisor-poc`, reached multi-user.target, started `advisor-os.service`, and accepted `advisor` / `advisor-poc` login.
- **Evidence:** `projects/advisor-os/artifacts/test-logs/qemu-boot-skip-07.log` and `projects/advisor-os/artifacts/test-logs/qemu-installed-verify-07.log`. Build logs are under `projects/advisor-os/artifacts/test-logs/`.
- **Current POC workaround:** `installer/Containerfile` sets `TMPDIR=/mnt/sysimage/boot`; the temporary `installer/bootc-wrapper.sh` binds target `/boot` over installer `/var/tmp` during payload import, unbinds after `Deploying container image...done`, and adds bootc's supported `--skip-finalize` so Anaconda owns final cleanup. Interactive defaults use 16 GiB `/boot` and encrypted LUKS2 root. `run-qemu.sh` defaults ISO installs to 80 GiB and 8 GiB RAM.
- **Next move:** Run the graphical/manual ISO installation test from `QEMU_MODE=iso ./projects/advisor-os/scripts/run-qemu.sh` using a fresh disk, then test KDE/SDDM, Brave/PWA, RPC, and printer workflow. After that, separately design and test a supported replacement for the wrapper/large-boot scratch/`--skip-finalize` path before calling this release-ready.
- **Tried and rejected:** A real `/var` partition did not appear as `/mnt/sysimage/var` in this Anaconda bootc path and produced `No such file or directory`; a separate `/tmp` partition was explicitly rejected as unsupported user-specified mount point. Binding `/boot` to `/var/tmp` fixed import ENOSPC; leaving it bound caused read-only finalization failure, so the wrapper unbinds after image import. `TMPDIR` alone did not move the image importer off installer `/var/tmp`.
- **Preservation rules:** Do not stage, overwrite, stash, or delete unrelated unrecognized content. Preserve `docs/ADVISOR_OS_LANDING_CONTENT.md`, the knowledge files, and ignored diagnostics. Do not claim final release acceptance until the manual GUI/KDE/PWA/printer checks pass and the temporary workaround has a reviewed replacement or explicit POC sign-off.
- **Build/test state:** No build or QEMU process is running. Latest handoff commit is `7fb1971`; current source/live-test changes are not yet committed after the working loop.
