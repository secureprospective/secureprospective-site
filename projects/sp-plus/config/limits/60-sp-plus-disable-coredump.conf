# A core dump of Brave contains whatever the advisor was reading: client names,
# policy numbers, account balances, in plaintext, in a file on disk that nothing
# encrypts beyond LUKS and nothing ever deletes. The crash-reporting value on a
# fleet with no crash-reporting pipeline is zero.
* hard core 0
* soft core 0
