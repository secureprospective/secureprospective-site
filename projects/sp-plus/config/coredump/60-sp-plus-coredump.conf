# systemd-coredump resets kernel.core_pattern to itself at boot, so a sysctl
# alone does not disable core dumps -- it only changes who receives them. This
# is the half that stops them being written at all.
[Coredump]
Storage=none
ProcessSizeMax=0
