# RESUME — SP+ 0.11 (v0.11.4) release: ghcr DONE, R2 uploading, Dell untouched
**Written:** 2026-09-13 ~03:45 EDT · supersedes RESUME-2026-09-12-v0.11.3-tested.md

## 1. WHAT WE ARE DOING
Releasing v0.11.4 as public **0.11**. Build, install-test, ghcr publish and signing are
DONE and verified. The ISO is mid-upload to R2. The Dell has not been updated yet.
**Active goal (a Stop hook enforces it):** "get 0.11 up and uploaded to cloudflare, do the
update on the Dell as if it was an advisor, verify everything before the goal is met."

- Repo (Beelink, ACTIVE): `~/work/secureprospective-advisor-os`, subdir `projects/sp-plus`
- Branch `session/sp-plus-defense-in-depth` — **never merge to main; main IS the live website**
  (`secureprospective-site.git`, Astro on Cloudflare Pages, deploys on push, we are ~605 ahead)
- Beelink: `ssh -i /root/.ssh/beelink chris@192.168.1.190`

## 2. IN-FLIGHT RIGHT NOW — two things
**A. R2 upload of the 5.2 GB ISO.** Started 2026-09-13 03:35 EDT, ~1 MB/s, **ETA ~1h30m**.
- alive? `pgrep -af "rclone.*spplus-releases"`
- log: `~/logs/sp-plus/r2-upload.log` (use `tr "\r" "\n" | tail -3`; it is a progress bar)
- target: `spplus-releases/sp-plus/sp-plus-0.11.iso`
- on success it self-verifies size and PRINTS THE releases.ts ENTRY. Capture that.
- if it died: re-run `bash projects/sp-plus/scripts/publish-iso-r2.sh upload` — multipart,
  resumable, and it aborts a failed upload explicitly rather than leaving billed orphan parts.

**B. VM `spplus-updatetest`** (v0.11.3, running, ssh port 2222) staging the v0.11.4 update.
- `ssh -i ~/.ssh/spvm -p 2222 test@127.0.0.1` FROM THE BEELINK
- `sudo /usr/libexec/spplus-update-control status` — was "available", staging via
  `systemd-run --unit=spplus-manual-stage`. Check `systemctl is-active spplus-manual-stage`.
- Purpose: rehearse the DELL's exact path before Christopher touches the Dell.

## 3. DONE AND VERIFIED THIS WINDOW
- **v0.11.4 ISO**: 5,520,687,104 bytes,
  sha256 `a793df68e7f21cea274d139ef9b3aca375f7545bba9a38ddb79945d863cc562b`,
  payload `f80a23f8cb3d`. `BUILD_ISO_OK` (ISO embeds the ref its kickstart names).
- **ghcr PUBLISHED + SIGNED + VERIFIED.** `:latest` and `:20260913` =
  `sha256:286075671c3f18259db7339c059f74ffdc389604f79e4b3c99e6ececd29304ac`.
  Fleet policy ACCEPTS it; the same digest is REFUSED under an unrelated key (mutation-tested).
- **Favourites fix PROVEN VISUALLY** — new `advisor` account, first login, launcher shows
  Brave, Fin, Writer, Dolphin, Okular, KeePassXC. No Konsole/Discover. Screenshot:
  `~/logs/sp-plus/shots-v0.11.4/launcher.png` (also sent to Christopher).
- **Defect B fixed**: `fin --help` is Fin's own page, names nothing upstream. `FIN_HELP_GATE_OK`.
- **Defect C fixed**: `{}` auth.json → "Fin is not connected yet.", rc=1. `FIN_AUTH_GATE_OK`.
  Six fixtures; both gates mutation-tested (revert the fix → gate fails).
- Partition polkit rule installed; small text fixes (one playbook, ledger hyphen).

## 4. THE RUNBOOK — READ IT INSTEAD OF RE-DERIVING
**`projects/sp-plus/docs/RELEASE-RUNBOOK.md`** (commit `4e9d7f5`). Christopher's explicit
instruction this window: *"we dont need to keep relearning the same process over and over."*
Eight stages + a traps table. Memory card: `reference_spplus_release_runbook`.

## 5. R2 FACTS (were expensive to find)
- Bucket **`spplus-releases`**, Pages project `secureprospective-site`, binding
  `SPPLUS_RELEASES` in BOTH Production and Preview. Private; the Function streams it.
- Account `002dd2f758b67ac08d05a3809d65a25a`; endpoint
  `https://002dd2f758b67ac08d05a3809d65a25a.r2.cloudflarestorage.com`, region `auto`.
- Credentials: `~/.config/sp-plus-r2/env`, mode 600. Account token, Object Read & Write,
  scoped to that one bucket. **The bucket was EMPTY before this upload.**
- ⚠️ **The Access Key ID and Secret were exposed in a `ps` listing** (the first version of
  publish-iso-r2.sh passed them as rclone FLAGS). Fixed — they now go through the environment.
  **Recommend rotating that token after the upload finishes.** Not yet done, not yet discussed.

## 6. HYPOTHESES REFUTED — DO NOT RETEST
| Claim | Verdict |
|---|---|
| "We already have Cloudflare tokens for this" | **REFUTED, checked twice.** `~/.cloudflare_token` (both machines) 403s on `/r2/buckets` AND `/pages/projects`. The wrangler OAuth login has scopes `workers:*`, `d1`, `pages:write` — **no `r2` scope at all** — and wrangler refuses OAuth non-interactively. A new R2 token was required and has now been made. |
| "publish-image.sh can just be re-run" | **REFUTED.** It refuses an image not newer than the tag; once `:latest` moved, a re-run fails. Finish signing by hand (runbook stage 5). |
| "The publish script is still running" | **REFUTED.** My own `pgrep -f publish-image.sh` matched its own ssh command string. It had already exited. [[lesson_remote_pgrep_matches_ssh_wrapper]] |
| "Two display managers are enabled — a defect" | **REFUTED, self-inflicted.** sddm was NOT running; my `systemctl restart sddm` started it. The greeter is **plasma-login-manager**; `/etc/sddm.conf.d` is silently ignored. Reverted. |
| "The policy accepted an unsigned image, so the check is blind" | **REFUTED.** `:20260911` is itself signed. Real mutation test = WRONG KEY, which is correctly refused. |
| Dell at 192.168.1.124 (homelab/CLAUDE.md) | **WRONG.** It is **192.168.1.234** (same MAC as .228 earlier — DHCP moved it twice). homelab/CLAUDE.md still needs correcting. |

## 7. DECISIONS MADE THIS WINDOW
- **D: version string stays `SP+ 1 (20260913)`.** Christopher chose "leave it — publish now"
  over rebuilding to read 0.11. 0.11 is how we refer to the release, not what the image says.
  Honours D-01 (round integer + dated BUILD_ID). Do not relitigate.
- **Only ONE thing ever goes to git main from a release**: the single `releases.ts` entry.

## 8. LEDGER STATE
Branch `session/sp-plus-defense-in-depth`, pushed, HEAD == @{u} == `4e9d7f5`.
This window: `7e77ef7` (Fin B+C + gates + small fixes), `4e9d7f5` (runbook + publish-iso-r2.sh).
**UNCOMMITTED:** the secret-out-of-argv fix to `publish-iso-r2.sh` and the bucket-name/default
edits to it and the runbook. Commit them — deliberately not committed mid-upload.

## 9. NEXT ACTIONS, IN ORDER
1. **Answer Christopher's open question**: he said *"we have to delete the old one"*. The R2
   bucket was EMPTY, so it is not an old ISO there. Candidates given to him: the `1.0-alpha`
   placeholder entry in `releases.ts` (published:false, size 0), an ISO hosted elsewhere, or
   old ghcr tags. **He has not answered. Do not delete anything until he says which.**
2. **Wait out the R2 upload**, then capture the printed `releases.ts` entry.
3. **Commit** the uncommitted script/runbook edits.
4. **Add the release entry to `main`** of the site repo — ONE small commit, `published: true`,
   after the object is confirmed in R2. Pages deploys on push.
5. **Rotate the R2 token** (see §5) and re-run `publish-iso-r2.sh check`.
6. **The Dell.** 192.168.1.234, powered on, **zero open ports** (correct: SP+ ships sshd off).
   User `dell`, password `password`, LUKS passphrase `password`. Christopher must do the
   advisor click-through at its keyboard (notification → restart button → passphrase), then
   `sudo systemctl start sshd` so it can be verified from here; stop sshd afterwards.
   Relay for this was written but `~/Downloads/paste.md` has since been OVERWRITTEN with the
   R2 credential steps — **rewrite the Dell batch before asking him to do it.**
7. Finish the `spplus-updatetest` rehearsal (§2B) — it is the Dell's path, risk-free.

## 10. ENVIRONMENT NOTES
- `paste.md` → `chris@192.168.1.190:/home/chris/Downloads/paste.md`. One batch per file.
- **Nested ssh + heredoc + apostrophes breaks quoting** — it truncated a commit message this
  window. Write a script/message FILE and `scp` it. Every single time.
- Beelink sudo is NOPASSWD for **podman only**. Root-owned artifacts were deleted by running a
  throwaway container with just that directory mounted.
- Two registry logins are needed: rootful podman AND user skopeo (different auth files).

## 11. HONEST STATUS
ghcr is real and verified — **existing machines can update right now.** Everything else is
either in flight (R2) or untouched (the Dell). **The advisor update path has NOT been observed
end to end on any machine yet**; `update-control check` correctly seeing the new digest is the
furthest it has been proven. Do not report the goal met until a machine has actually booted
`sha256:286075…` and the members page serves the ISO.
