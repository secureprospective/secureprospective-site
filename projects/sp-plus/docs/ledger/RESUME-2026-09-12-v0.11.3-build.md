# RESUME — SP+ v0.11.3 build, carrying four fixes found by running the product

**Written:** 2026-09-12, second compaction of this session. The session CONTINUES.
**Christopher's instruction:** "apply all of the new fixes and after compact we will build the
new ISO with the fixes."

---

## 1. WHAT WE ARE DOING

Building the next SP+ ISO. The v0.11.2 ISO already exists and is sound; four fixes have landed
since it was built, three of them found by driving the shipped product rather than reading it.
The next build is the release candidate intended to go public as **v0.11**.

- **Repo:** `chris@192.168.1.190:~/work/secureprospective-advisor-os`
- **Branch:** `session/sp-plus-defense-in-depth` — stay on it, do NOT branch
- **Build context:** `projects/sp-plus`, NOT the repo root
- **Build command:** `scripts/build-iso.sh <tag>` from `projects/sp-plus`
- **Host:** `ssh -i /root/.ssh/beelink chris@192.168.1.190`

⚠️ `~/work/sp-plus` also exists on the Beelink and is NOT the repo. It is a folder of evidence
and captures. `cd ~/work/sp-plus` then `git log` fails with "not a git repository". Two minutes
were lost to this today.

## 2. AGENTS + HARNESSES

| Who | What | State |
|---|---|---|
| **Tom** | Claude Code on the Beelink, pid **26754**, ~3h45m elapsed | **ALIVE and is Christopher's.** Working on the **WEBSITE, not SP+** — he said so explicitly. Earlier worry about branch collision is settled: leave it alone. |
| **Bee** | not used this window | idle |

**Check Tom:** `ssh -n -i /root/.ssh/beelink chris@192.168.1.190 'ps -p 26754 -o pid,etime,args'`

## 3. GATES / STATUS — all green, tree clean

| Gate | Result | How to run (from `projects/sp-plus`) |
|---|---|---|
| `tests/config-preflight.sh` | **41 passed, 0 failed — "Safe to build."** | `bash tests/config-preflight.sh` |
| `tests/fin-extension-gate.mjs` | **151 pass, 0 fail** (was 142) | `node tests/fin-extension-gate.mjs` |
| `tests/fin-permissions-gate.sh` | PASS | `bash tests/fin-permissions-gate.sh` |
| `tests/fin-notebook-recall-gate.mjs` | **19 pass, 0 fail** — NEW this window | `node tests/fin-notebook-recall-gate.mjs` |
| `tests/theme-phase2-source-gate.sh` | PASS, +4 new cursor assertions | `bash tests/theme-phase2-source-gate.sh` |
| Live Fin probe of guardrails + data boundary | **DONE this window.** See §6. | — |
| Live probe of the notebook changes | **NEVER RUN** — the VM predates them | — |

The preflight prints `DO NOT BUILD` the moment the tree is dirty. That is the only thing that
made it fail all day; every red it showed was real.

## 4. ARTIFACTS THAT EXIST

- **v0.11.2 ISO** — `beelink:~/Downloads/SP-PLUS-v0.11.2.iso`
  - 5,516,963,840 bytes
  - sha256 `979c0b1e5f3ed3701f5da2a02e8e8ef9053225cd80cf5abe32ca7e13b5a25586`
  - payload `localhost/sp-plus-kde:v0.11.2`, image id `59dd519d91dcee814f03b82198c27c2f3920eb7f31988786dd1b5ea13d6822c6`
  - build log `~/logs/sp-plus/build-v0.11.2-20260912T173744Z.log`
  - **This ISO predates all four fixes below.** It is good but superseded.
- **v0.11.1 ISO** — `~/Downloads/SP-PLUS-v0.11.1.iso`, sha256 `a8be450ce701aef93452900c303fa7a4ff7c894dfebc56d24dc7a6cb495ce34a`
- **Live-probe evidence, 12 screenshots** — `~/logs/sp-plus/2026-09-12-live-fin-probe/`
  Filed there this window; they were in /tmp and would have been lost.
- **VM `fedora-0.11.2`** — RUNNING under `virsh -c qemu:///session` on the Beelink. Built from
  the v0.11.2 ISO. Fixtures created during probing were deleted (`~/.env`, `~/.ssh/id_rsa`,
  `~/Documents/client-review.md`).

## 5. CURRENT STATE — no bug, nothing broken

Nothing is failing. The work is "four fixes committed, none of them in an image yet."

Still open and NOT addressed this window:
- **Publishing to ghcr is still overdue.** `ghcr.io/secureprospective/sp-plus-kde:latest` serves
  build 20260910. Three builds behind now.
- **The data-access log writes an em dash.** Seen live: `- 2026-09-12 18:57 — approved by the
  advisor: /var/home/alpha/Documents/client-review.md`. This project gates advisor-facing text
  against em dashes everywhere else. Cosmetic, in a file the advisor can open. Not fixed.
- **Disk recovery key** promised in four documents, never created by the installer. Christopher's
  call: build it or strike the copy.
- **Two SP+ global themes** still in the image that an earlier decision said to drop.
- **The posture gate's vouched-app-store check is defective** — counts installed apps, will cry
  wolf on every real machine.
- **Dolphin → "Files" rename** decided, still NOT implemented.

## 6. HYPOTHESES ALREADY REFUTED — DO NOT RETEST

Carried forward from the previous resume, still true:

1. **Do NOT merge this branch to `main`.** `main` is the live SecureProspective website (Astro on
   Cloudflare Pages, deploys on push). Diverges by 6,658 files.
2. **Do NOT run `bootc upgrade` on the VM.** The published image is OLDER; accepting downgrades it.
3. **Reading the guardrail rules does not find holes.** Proven twice more today.
4. **Nested ssh heredocs mangle multi-line text.** Bit me again this window on a `python3 - <<PY`
   through ssh: `syntax error near unexpected token '('`. **scp the script as a file and run it.**
   This is the single most repeated mistake of the session.
5. **The gate's `jiti` import must be an absolute path.** On the Beelink,
   `/home/chris/.local/lib/node_modules/@earendil-works/pi-coding-agent/node_modules/jiti/lib/jiti.mjs`.
   Inside the image it is `/usr/lib/node_modules/...` — sed it when running a gate in a container.
6. **`\b` before a dot never matches.**
7. **Keyword presence in a file is not coverage.**

**New this window, and expensive:**

8. **A model refusing is NOT the guardrail firing.** Asked directly to show an ssh private key,
   Fin declined on its own with no tool call. That proves nothing about the extension. The way
   to test the boundary is to ask for something the model has no reason to refuse — a mail host
   out of `~/.env` — at which point the model complies, issues the tool call, and the extension
   blocks it. **Only a probe that produces a tool call is evidence.** This is the same error that
   produced four false positives in the previous session's scoring.
9. **The v0.11.2 VM cannot be reached by SSH, and that is CORRECT.** sshd ships off, there is no
   guest agent, and there is no hostfwd on the user-mode NIC. `virsh console` refuses without a
   TTY. **The working channel is `virsh send-key` plus `virsh screenshot`.** Helpers were written
   at `/tmp/vmkeys.sh` and `/tmp/vmshot.sh` on the Beelink and have since been reaped; rewrite
   them if needed, they are ~40 lines. Alt+Tab must be sent as ONE chord
   (`virsh send-key <dom> --holdtime 120 KEY_LEFTALT KEY_TAB`); sending the two keys in sequence
   does nothing.
10. **Inspecting the payload image inspects the VM.** When the VM is unreachable, run gates
    against `localhost/sp-plus-kde:<tag>` with podman. The ISO verification proves the embedded
    payload id matches, so the image IS the bits on the VM.
11. **`fin-permissions-probe.cjs` and `fin-extension-gate.mjs` have DIFFERENT case formats.**
    The probe uses `["tag", "command"]`; the gate uses `['command', 'why']`. Patching the wrong
    shape into the wrong file fails the anchor assert.

## 7. DECISIONS (do not relitigate)

- **v0.11.1 is in the books.** The next ISO is the public **v0.11**.
- **Fin is "a tool, not a destroyer"** — thorough over permissive.
- **Notebook recall, decided 2026-09-12:** the voice profile is carried in full at session start
  so the first draft sounds right with no lookup; everything else in the notebook is named, not
  loaded.
- **Sessions auto-save on a threshold.** Neither persona (the advisor who pecks all day, the
  marketer in one long session) will ever file paperwork, so the save rides on pi's compaction
  summary, which has already been paid for.
- **Roadmap:** Bee's thesis wording and third class; Tom's 28-item catalogue and sequencing.
- **Build order:** data boundary (DONE) → `filing` → `follow-ups` + tickler → broker after D1.

**D1 — OPEN and gating.** The system prompt promises client data never leaves the machine; Fin is
cloud-first on a ChatGPT account. Note: the data boundary's live prompt now states the truth out
loud — "sends what is inside it to the service that answers for Fin" — which narrows D1 to the
system prompt's wording rather than the behaviour.

## 8. LEDGER STATE — all committed AND pushed, tree clean

| Commit | What |
|---|---|
| `258af64` | two permission questions did not say what happens (wording; gate had never been run) |
| `262aa52` | Fin stops charging the advisor for recall it did not ask for (the notebook rework) |
| `c74188e` | a permission warning that was not true (the `--help` false positive) |
| `5aab62e` | the look chooser now says it finished (the spinner) |

Nothing uncommitted. Nothing unpushed. `git status -sb` clean against origin.

## 9. NEXT ACTIONS, IN ORDER

1. **Build the ISO.** From `projects/sp-plus` on the Beelink:
   `nohup setsid bash scripts/build-iso.sh v0.11.3 > ~/logs/sp-plus/iso-v0.11.3-wrapper.log 2>&1 < /dev/null &`
   Takes ~29 minutes. Watch the log for these four markers, all of which are NEW or newly
   meaningful and none of which has ever executed:
   - `FIN_SEARCH_TOOLS_OK` — ripgrep and fd are present, so Fin stops warning on launch
   - `FIN_NOTEBOOK_RECALL_OK` — the index self-heals, recall is a cached primer
   - `FIN_DATA_BOUNDARY_OK` — reads held on both launch paths
   - `Image build successful:` then `BUILD_ISO_OK`
   If any marker is absent the build is a draft, whatever the exit code says.
2. **Copy the ISO to `~/Downloads/SP-PLUS-v0.11.3.iso`** and record its size and sha256.
   The build writes to `artifacts/<tag>-iso/...` owned by root but world-readable; a plain `cp`
   as chris works.
3. **Install it to a fresh VM and confirm by eye, in this order:**
   - Launch Fin. **No yellow warnings, no red ENOENT.** That is the whole point of `262aa52`.
   - Say "hello". The notebook skill must NOT load and nothing must be read.
   - Choose the look, apply one, then **hover the apply button** — the pointer must return to
     normal and the button must read APPLIED in solid blue with a check. That is `5aab62e` and it
     is the only fix today with NO runtime evidence at all.
   - Ask Fin to reinstall the file manager. It must research with `--help` **without** a false
     warning, and must still refuse the actual removal.
4. **Publish to ghcr** — standing order, three builds overdue.
5. Then the remaining cheap items in §5, then `filing`, then `follow-ups` + tickler.

## 10. RELAY / ENVIRONMENT

- **Beelink:** `ssh -i /root/.ssh/beelink chris@192.168.1.190`
- **Driving the VM (no SSH into it — see §6.9):**
  - `virsh -c qemu:///session screenshot <dom> /tmp/x.ppm` then `convert` to png, scp back, Read it
  - `virsh -c qemu:///session send-key <dom> KEY_A` for keys; chords need one call with `--holdtime`
- **Running a gate inside the image:**
  `sudo -n podman run --rm -v "$PWD/tests:/gate:ro,Z" --entrypoint /bin/bash localhost/sp-plus-kde:<tag> -c '...'`
  — sed the jiti path to `/usr/lib/node_modules/...` and set `HOME` to a writable dir, because the
  extensions create `$HOME/Documents/Fin` on load and `/root` is not writable in the container.
- **Editing files on the Beelink from CT105:** write a Python patch locally, `scp` it, run it.
  Never pipe a heredoc through ssh (§6.4).
- **paste.md relay:** write on CT105, scp to `chris@192.168.1.190:/home/chris/Downloads/paste.md`

## 11. HONEST STATUS

**Four fixes are committed and none of them has run inside an image.** Every gate is green and
every gate is source-level or in-process.

Specifically unproven:
- **The notebook rework has never run against live Fin.** Its 19 assertions drive the real
  handlers in-process, and I mutation-tested them four ways, but that is not the same as an
  advisor typing "hello" and seeing a clean screen.
- **The cursor fix has never been rendered at all.** There is no display on the Beelink and the
  running VM predates the change. It is deterministic CSS with four gate assertions behind it,
  but nobody has looked at it.
- **The `--help` fix was verified in-process**, not by re-running the live scenario that found it.
- **The data boundary remains a floor, not a fence.** It stops a careless advisor and a
  cooperative model. It does not stop a hijacked one.

What IS proven, live on the VM this window: refusal is final and Fin does not escalate; the data
boundary fires on a real read, defaults to No, and logs the decision; the secret class is enforced
mechanically against a cooperative model that genuinely tried to comply.

**ETA:** ~30 minutes to the ISO, then the eye-check in §9.3 is the gate that matters.
