# SP+ Operations Ledger — how the build team works

`DO-NOT.md` records facts about the **product**. This file records lessons about the
**process**: how CT105 (Headbrain), Bee/Luna (build + research), and Tom (independent
verification) work together, and how that goes wrong.

SP+ will be rebuilt many times over several years, by people and agents who were not here.
Product bugs are cheap to rediscover; process failures are not, because they waste whole
sessions and they recur silently. Every entry below cost real time on 2026-08-26.

**Format:** each entry states the failure, the cost, and the rule that prevents it.
An entry earns its place by having actually happened. Do not add predictions here.

---

### OP-01 — Research the failure before touring hypotheses

**What happened.** An installer ISO hung before Anaconda. CT105 began serially testing its
own guesses: SATA vs USB attach, then a GRUB command-line edit. Two cycles burned, no
diagnosis. Christopher stopped it: *"before going on a guessing tour of problems, use Luna
to research the web to give you similar experiences and fixes."*

**Cost.** ~25 minutes and one invalid test (the GRUB edit landed on the wrong line).

**Rule.** For any failure in third-party tooling, **research before hypothesis**. Someone
has almost certainly hit it. Dispatch research with the full evidence — verbatim errors,
artifact contents, exact configuration — and require citations. A hypothesis tour is only
justified when the problem is genuinely ours and unpublished.

### OP-02 — An agent's account of WHY something failed is a claim, not evidence

**What happened.** Bee reported *"sudo elevation was blocked by the execution environment"*
and stopped. `sudo -n podman` worked fine and had already been used successfully earlier
the same day. The real cause was that Bee had built rootless and tripped the rootless/root
store split (DN-06) — an entry Bee itself had written that morning.

**Cost.** One full dispatch.

**Rule.** Error TEXT is evidence. The causal STORY attached to it is a claim, and it is
generated by the same process that failed. Verify the mechanism independently before acting
on the explanation. Ask: what would be true if this story were correct, and is it?

### OP-03 — Verify the tree, never the report

**What happened.** Standing practice, and it held. Every gate was confirmed by inspecting
the repository, the artifact, and the running system from CT105 BEFORE reading the agent's
report. A `WORKS.md` entry count looked wrong; inspection showed it was a format template
inside a fence, not a fabricated entry.

**Rule.** THE ARTIFACT IS THE EVIDENCE, NEVER THE EXIT CODE, AND NEVER THE REPORT. Check
the tree first, then read the report to see whether it matches. Reading the report first
anchors you to its framing.

### OP-04 — A dispatch harness that can fail silently is worse than none

**What happened.** `run-tom.sh` v1 passed the prompt through
`ssh` -> `setsid bash -c '...'` -> `printf %q`. The nesting mangled it, the command never
executed, and there was no process, no output file, and no sentinel. It was
indistinguishable from an agent thinking hard. Ten minutes were spent waiting on nothing.
The same class of bug bit again hours later when a heredoc was nested through `ssh` to
write this very file.

**Cost.** ~10 minutes, plus a wrong public statement that Tom was dead before confirming
Tom could run at all.

**Rule.** Ship a runner SCRIPT or `scp` a file; never nest quoting or heredocs through ssh.
And every dispatch harness must **prove the job started** — check for the process or its
output within seconds and report `FAILED TO START` loudly. Silence must never be a possible
success state.

### OP-05 — Match the harness to the task

**What happened.** `run-bee.sh` carries a fixed pointer ending *"write nothing to disk"* —
correct for research, fatal for a build brief that must create and commit files. It was
caught 20 seconds after dispatch only because the running process was inspected.

**Rule.** Harnesses encode assumptions. Read the actual prompt a harness sends before using
it for a new class of work. `run-bee.sh` = research. `run-bee-build.sh` = build.

### OP-06 — Commit only what the brief names

**What happened.** A build dispatch committed 455 lines of unrequested documentation
(`b141d7b`) in the middle of a failing build.

**Rule.** Every brief states: commit ONLY files this brief names; anything else is a
finding to REPORT, not a commit to make. Off-brief work buries the signal in the diff and
makes a failed dispatch hard to unwind.

### OP-07 — Name the detection signal for silent failures

**What happened.** The ISO hang produced no error anywhere: GRUB succeeded, the kernel
loaded, and a splash spinner animated forever behind `rhgb quiet`. Nothing failed loudly.
**The signal that revealed it:** the target disk never grew past ~197 KB while CPU stayed
at 20-45%, and three screendumps minutes apart were byte-identical.

**Rule.** For any failure class with no error text, the ledger entry MUST carry a
`Detect with:` field naming the command or observation that exposes it. Tom identified this
gap in the scaffold review before it cost anything. Busy CPU plus a static artifact plus an
unchanging screen is a HANG, not slow progress.

### OP-08 — Do not pronounce a component dead before testing whether it can run

**What happened.** On finding no process and no output for Tom, CT105 told Christopher
"Tom is dead". The correct statement was "no evidence Tom started". The diagnosis happened
to be right, but it was asserted before the one cheap test (can `claude -p` run at all?)
had been done.

**Rule.** Report what the evidence supports, at the strength it supports. "No evidence of
X" and "X failed" are different claims and cost different things when wrong.

### OP-09 — The instrument before the measurement

**What happened.** Christopher's plan is to install SP+ in QEMU, then on the Dell, and feed
both examinations into the next build. Ad-hoc examination of each would produce two sets of
impressions that cannot be compared.

**Rule.** When two environments will be compared, build ONE inspection script and run it in
both. Fixed section order, fixed key names, a closed status vocabulary
(`OK`/`PROBLEM`/`UNKNOWN`/`SKIPPED_NEEDS_ROOT`), and all volatile values quarantined in a
single block so the diff is readable. The comparison is the deliverable, not the reports.

### OP-10 — Every PROBLEM gets promoted before the next build

A problem observed in a field report and not promoted to a `T-nn` or `DN-nn` is a problem
that will be rediscovered. Promotion happens BEFORE the next build begins, not after.

### OP-11 — Give the agent a thrash limit and a hypothesis requirement

**What happened.** Dispatches that carried "at most N attempts, each with a hypothesis
stated BEFORE the command runs" produced clean, useful failure reports with eliminated
hypotheses. The dispatch without that framing produced three variations on fighting a
symptom (`chcon`) instead of the cause.

**Rule.** Every build brief names a hard attempt cap and requires a written hypothesis per
attempt. Three eliminated hypotheses is real progress; thirty blind retries is a lost
session. State plainly in the brief that an accurate blocker reported early is worth more
than a thrashed session.
