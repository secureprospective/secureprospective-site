{
  "name": "impeccable-detector-deps",
  "private": true,
  "description": "Parser dependencies for the Impeccable detector. Node resolves these by walking up from scripts/detector/. Without them the detector silently degrades to regex matching and undercounts findings.",
  "dependencies": {
    "css-select": "^5.2.2",
    "css-tree": "^3.2.1",
    "domutils": "^3.2.2",
    "htmlparser2": "^10.1.0",
    "puppeteer": "^25.9.0"
  }
}
