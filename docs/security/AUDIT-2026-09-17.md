# secureprospective.com security audit — 2026-09-17

Scope: every Pages Function on origin/main (aa593f9), members pages, `_headers`, migrations,
git history, dependencies, DNS/TLS, live production and live preview deployments.
Probes were unauthenticated and non-destructive. No forms were submitted.

NOT covered (browser extension disconnected; wrangler OAuth lacks Pages scope):
Pages bindings/secrets per environment, Cloudflare Access, WAF/rate-limit rules, SSL mode,
DNSSEC, account members/2FA, API token list, D1 contents.

## Findings, most severe first

| # | Sev | Where | Finding | Evidence | Fix |
|---|-----|-------|---------|----------|-----|
| 1 | HIGH | Pages previews | Old branches run live on the public internet with Functions: `session-advisor-os-poc`, `bee-impeccable-setup` (kit wizard, Google OAuth, `dev-bypass` that fakes Google auth on every non-main branch, `/api/ask`), `session-contact-forms`, `session-watercolor-portrait`. Every pushed branch, including agent `bee/*` branches, becomes deployed code holding whatever preview secrets and bindings exist. | `/api/wizard/status` 401 JSON and `/api/google/oauth/start` 302 on the advisor-os-poc preview | Delete stale deployments. Put Cloudflare Access (free) on `*.secureprospective-site.pages.dev`. Limit preview builds to chosen branches. Confirm preview bindings do not point at the production D1 or R2. |
| 2 | HIGH | `api/download/iso.ts:1119` | The member-only ISO is sent with `Cache-Control: public, max-age=31536000, immutable`. A shared cache or edge cache rule can serve it to people who are not logged in. | code | `private, no-store` (or `private, max-age=…`) |
| 3 | MED | `_lib/session.ts` + all APIs | `must_change_password` is enforced only in page JavaScript. A one-time password (bootstrap or admin reset) session can call `/api/download/*` and every admin API directly. | `getSession` never checks it | Refuse every route except `me`, `change-password` and `logout` while the flag is set |
| 4 | MED | `api/auth/change-password.ts:618` | Changing your password kills only the current session. A stolen cookie stays valid for up to 30 days, renewed while in use. | code | `DELETE FROM sessions WHERE user_id = ?` before issuing the new session |
| 5 | MED | `_lib/http.ts:45` | The origin lock accepts any `*.pages.dev`, so anyone's Pages site passes it. SameSite=Lax softens this, but the lock is meaningless as written. | foreign `evil.pages.dev` Origin got 401 (passed), not 403 | `.secureprospective-site.pages.dev`, as `lead.ts` already does |
| 6 | MED | login, accept-invite, change-password | No rate limiting or lockout. Login relies only on Turnstile, and change-password has none. The minimum password is 8 characters, with no breached-password check. | code | Cloudflare rate-limit rule on `/api/auth/*` (the free tier allows 1 rule); minimum 12 characters |
| 7 | MED | `members/admin/index.astro:270,314,414` | Stored XSS in the admin console: emails and `created_by` go into `innerHTML`. The `EMAIL_RE` check accepts `<img/src=x/onerror=…>@a.co`, and the CSP allows `'unsafe-inline'`. Only an admin can plant it, or a stolen admin session. | code | `textContent` for data cells |
| 8 | MED | deps | `pnpm audit --prod`: 1 critical, 15 high, 16 moderate, 4 low. Astro 4.16.19 has advisories fixed in 7.x. Most matter only at build time or SSR, since the site is static plus Functions. | audit output | Upgrade Astro and rerun the build and tests |
| 9 | LOW | `api/auth/admin/bootstrap.ts` | Still deployed and still backed by `ADMIN_BOOTSTRAP_KEY`. It returns 409 once an admin exists, but it is attack surface that has no further use. | code | Delete the route and the secret |
| 10 | LOW | `/members/sp-plus-security-architecture/` | A 151 KB security report is public under `/members/` with no gate. `inside-sp-plus` is public on purpose (its comment says so); this page carries no such note. | 200 unauthenticated | Christopher decides: public or gated |
| 11 | LOW | `api/auth/member-edit.ts` | A role or email change does not end the member's sessions. Role is read live, so demotion still takes effect. | code | Delete that user's sessions on edit |
| 12 | LOW | `_headers` | CSP `script-src 'unsafe-inline'` weakens #7. `ACAO: *` is on static pages (Pages default; no API impact). There is no CAA record. | headers, dig | Move inline scripts to hashed or external files; add CAA for the issuing CAs |
| 13 | LOW | DNS | `crm.secureprospective.com` is proxied and returns 404, and a certificate exists for `*.crm` and `pay.`. These are leftovers. | dig, crt.sh | Remove the DNS records if unused |

## Checked and clean
- Passwords use scrypt with a per-user salt and constant-time compare. Sessions use 256-bit random tokens stored as hashes, with HttpOnly, Secure and SameSite=Lax cookies.
- All SQL is parameterized. Every admin route calls `requireAdminSession`. No admin can demote or remove their own account.
- The ISO, list, me and admin routes return 401/403 without a session (verified live). R2 is private: the old token returns 401 and the new one is scoped.
- Git history holds no secrets. The `0x4AAA…` strings are 24-character Turnstile site keys, which are public.
- Lead email HTML is escaped. The lead form uses Turnstile, fails closed, and has length caps.
- SPF, DKIM and DMARC `p=reject` are set on the root domain. HSTS, `frame-ancestors 'none'`, nosniff and Referrer-Policy are present.
- No `.git`, `.env`, source, or `dist` exposure on production.

## Remediation status (branch `session/security-hardening`, preview only)

| # | Status | Evidence |
|---|--------|----------|
| 1 | OPEN: needs the Cloudflare dashboard (browser extension was disconnected) | Delete stale preview deployments; add Access to previews |
| 2,3,4,5,6(length),7,9,11 | FIXED | `tests/auth-hardening.test.ts`: 16 cases fail on origin/main and pass on the branch. Preview: foreign `*.pages.dev` Origin now gets 403, and bootstrap returns 404 |
| 6 (rate limit) | OPEN: dashboard rule | |
| 8 | FIXED | Astro 7.3.3. `pnpm audit --prod` finds no known vulnerabilities, and the built file set is identical to the Astro 4 build |
| 10 | FIXED | Root `functions/_middleware.ts`. 18 path variants on the preview all return 302 or 400 with no content leaked. The first folder-scoped gate was bypassable with `//`, `%2F` and `%6d` variants; tests now cover those |
| 12,13 | OPEN | CSP inline-script work, CAA record, `crm.` DNS cleanup |

Not yet observed: a logged-in member walking the preview (login, download, admin console, page transitions under Astro 7). That needs Christopher's login in a browser.

## Cloudflare account findings, 2026-09-17 (via the dashboard AI, read-only pass)

Its token expired before it could make any change, so Parts A to D of
`docs/security/CLOUDFLARE-AI-PROMPT-2026-09-17.md` are all still outstanding, as are the
cache-rule, member and token listings.

| Sev | Finding |
|-----|---------|
| HIGH | Preview and Production bind the **same** databases and buckets: `BACKOFFICE_DB fd3d4c74…`, `ECOSYSTEM_DB 76a9973f…`, `KIT_DB fd1b7594…`, `LEADS→ccwork-leads`, `SPPLUS_RELEASES→spplus-releases`, `SP_LEADS→secureprospective-leads`. Combined with `preview_deployment_setting: "all"`, any branch anyone pushes runs live, unauthenticated, against production member accounts, sessions, leads and releases. This is finding #1's real severity. |
| HIGH | 118 preview deployments are live. Several hold aliases and need a force delete. Keep `d72d90b9-9dcd-4df2-9cd7-9d3dbd02f983` (newest `session/security-hardening`). |
| MED | Production build is stuck: `NODE_VERSION=20` with Astro 7 needing >= 22.12. Preview has no `NODE_VERSION` at all, which is why the preview built. Production still serves `aa593f9`, the pre-audit code. |
| MED | Minimum TLS version is 1.0, and Always Use HTTPS is off. HSTS covers a browser that has already visited over HTTPS; a first plain-HTTP request is not redirected. |
| LOW | DNSSEC is disabled. |
| LOW | `crm.secureprospective.com` is an `AAAA 100::` proxied record backed by a Worker. No `pay`, `www.pay` or `*.crm` records exist, so the `pay.` certificate is historical. |
| — | Zone `de242726850bb77c998406e61022b2b1`, Free plan, SSL mode strict. |

## Account work actually performed, 2026-09-17 (by API token, verified)

| # | Change | Verification |
|---|--------|--------------|
| 1 | `preview_deployment_setting` set from `all` to `none`, include list emptied. `main` still auto-deploys. | Project read back `none` |
| 1 | All 120 stale preview deployments deleted (force, aliases included). One kept: `d72d90b9` (`session/security-hardening`). | 77 deployments remain, exactly 1 preview. `session-advisor-os-poc` and `bee-impeccable-setup` wizard URLs now 404 |
| 1 | Access app created for `*.secureprospective-site.pages.dev`, allowing `christopher@secureprospective.com` and `secureprospective@gmail.com`, 24h session. | Preview URLs 302 to the Access login. `secureprospective-site.pages.dev` and the apex still 200 |
| 6 | Rate limit rule `members-auth-rate-limit`: POST to `/api/auth/*`, 5 requests / 10s per IP+colo, block 10s. | Rule reads back enabled. **It does not block in testing**, 20+ bursts all 200 after 15 minutes. Unresolved. |
| 8 | `NODE_VERSION` set to 22 on Production and added to Preview. | Project read back 22/22. Astro 7 can now deploy from `session/astro-7-upgrade` |
| 9 | `ADMIN_BOOTSTRAP_KEY` deleted from Production variables. | Absent on read back |
| — | Minimum TLS raised to 1.2 and Always Use HTTPS enabled (Christopher, dashboard). | TLS 1.1 handshake refused, TLS 1.2 serves 200, `http://` 301s to `https://` |

### Also found on the account
- Workers: `sp-crm` on `crm.secureprospective.com`, `sp-unsub-mock` on both `unsubscribe.secureprospective.com` and the route `secureprospective.com/unsubscribe` (a Worker named "mock" answering on the live domain), and `cap-shield`, last modified 2026-07-04. None reviewed.
- 7 Pages projects on the account: `secureprospective-site`, `techfreedomministries`, `life-insurance-explainer`, `sp-concept-immersive`, `sp-concept-radical`, `tfm-playbook-lite` (serving `football-play-editor.pages.dev`), `tfm-playbook`.
- A `http_request_cache_settings` ruleset exists on the zone (id `8a290e9273a44336aec506c411b768d0`). Its rules were never read: the token lacked Cache Rules Read. This is still the leading explanation for HTML being cached at the edge for 4 hours.

### Still open
- DNSSEC is disabled, no DS record published.
- The rate limit rule does not take effect; cause unknown.
- Cache rules unread, so the 4-hour HTML caching is unexplained. If a rule forces caching on HTML, confirm the gated architecture report cannot be served from cache to an anonymous visitor.
- Preview and Production still bind the same D1 databases and R2 buckets. Auto-deploys and the Access lock mean nothing reaches them unattended now, but separate preview databases remain the real fix.
- `sp-unsub-mock` on the live domain, and the two other Workers, unreviewed.
- Account members with 2FA status, and the API token inventory, never listed. Both are user-level pages.
- CSP still allows `'unsafe-inline'` for scripts. No CAA record.
